export type BlogInputType = {
    // id: string
    name: string
    description:string
    websiteUrl: string
}

export type BlogViewType = {
    id: string
    name: string
    description:string
    websiteUrl: string
}